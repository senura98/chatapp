// by mentioning a code inside a .d.ts it is available throughoutthe app without needing to be imported

interface User {
  email: string;
  emailVerified: string;
  id: string;
  image: string;
  name: string;
}
